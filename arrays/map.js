/*let notas = [5, 6, 7, 8, 9, 10];

let notasAtualizadas = notas.map(item => item== 10? item : ++item

)
console.log(notasAtualizadas)
*/

let nomes = ['Lucas','Ju','Yan','Tiago'];

let nome = nomes.map(item=> item.toLocaleLowerCase());
console.log(nome);